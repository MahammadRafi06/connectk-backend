apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: connectk
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  # Points to the backend K8s Service (cluster-internal DNS)
  NEXT_PUBLIC_API_URL: "http://backend.connectk.svc.cluster.local:8000"
