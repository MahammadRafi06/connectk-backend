apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GKE + self-hosted Postgres & Redis
# Deploy: kubectl apply -k k8s/overlays/gke/self-hosted/

resources:
  - ../../../base
  - ../storageclass.yaml

components:
  - ../../../components/postgres-self-hosted
  - ../../../components/redis-self-hosted

images:
  - name: BACKEND_IMAGE
    newName: YOUR_REGISTRY/connectk-backend
    newTag: latest
  - name: FRONTEND_IMAGE
    newName: YOUR_REGISTRY/connectk-frontend
    newTag: latest
